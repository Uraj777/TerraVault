# TerraVault Environment Configuration
SECRET_KEY=dev_secret_key_change_in_production
# Default to local SQLite database for development; replace with Supabase PostgreSQL URL in production
DATABASE_URL=sqlite:///terravault.db
FLASK_APP=run.py
FLASK_DEBUG=True
